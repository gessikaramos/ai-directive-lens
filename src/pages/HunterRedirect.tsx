import { useEffect } from "react";

const HunterRedirect = () => {
  useEffect(() => {
    window.location.href = "https://hunter.lolalabstudio.com";
  }, []);
  return null;
};

export default HunterRedirect;
